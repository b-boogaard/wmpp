loops=300001;
denom=3;
pi=4.0;
addflop=1;
i=1;
while(i=(i+1)<loops);
else=1;
if(addflop==1);
pi=(pi-(4/denom));
addflop=0;
denom=(denom+2);
else=0;
endif;
if(else==1);
pi=(pi+(4/denom));
addflop=1;
denom=(denom+2);
endif;
endwhile;
pi